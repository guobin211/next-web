{
  "name": "@tencent/next-cli",
  "description": "辅助开发的命令行工具",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": "dist/bin.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf .turbo && rm -rf node_modules",
    "test": "jest"
  },
  "files": [
    "json",
    "dist"
  ],
  "devDependencies": {
    "@tencent/next-config": "workspace:*"
  }
}
